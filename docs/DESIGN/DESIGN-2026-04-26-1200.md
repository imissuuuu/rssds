---
rdd: RDD-phase1
title: フィード管理UI — 実装設計
created: 2026-04-26
status: draft
---

# DESIGN: フィード管理UI（Phase 1）

RDD: `docs/RDD/phase1-feed-management-ui.md`  
アーキテクチャ: See HANDOFF/architecture.md  
コーディング制約: See `.claude/rules/coding-patterns.md`

---

## 変更ファイル一覧

| ファイル | 変更種別 |
|----------|----------|
| `source/feed_config.h` | 関数追加 |
| `source/feed_config.cpp` | 関数追加 |
| `source/ui.h` | enum/struct 拡張 |
| `source/ui.cpp` | 描画・入力追加 |
| `source/main.cpp` | kRepeat 条件追加 |

---

## 1. feed_config.h / feed_config.cpp — saveFeedConfig 追加

### feed_config.h

```cpp
// 追加
bool saveFeedConfig(const std::string& path, const std::vector<FeedConfig>& configs);
```

### feed_config.cpp

```cpp
bool saveFeedConfig(const std::string& path, const std::vector<FeedConfig>& configs) {
    FILE* fp = fopen(path.c_str(), "w");
    if (!fp)
        return false;
    fprintf(fp, "{\n  \"feeds\": [\n");
    for (int i = 0; i < (int)configs.size(); ++i) {
        const auto& c = configs[i];
        // nameにダブルクォートが含まれる場合は省略（入力はURLのみ）
        fprintf(fp, "    {\"url\": \"%s\", \"name\": \"%s\", \"fetch_full_text\": %s}%s\n",
                c.url.c_str(), c.name.c_str(),
                c.fetch_full_text ? "true" : "false",
                i + 1 < (int)configs.size() ? "," : "");
    }
    fprintf(fp, "  ]\n}\n");
    fclose(fp);
    return true;
}
```

---

## 2. ui.h — 型・状態追加

### Screen enum 拡張

```cpp
enum class Screen {
    FeedList,
    Loading,
    LoadingAll,
    ArticleList,
    ArticleView,
    ImageView,
    Settings,
    LoadingArticle,
    BookmarkList,
    ManageFeeds,   // 追加
};
```

### 新規 enum — FeedListPopup

```cpp
enum class FeedListPopup { None, Menu, Move, DeleteConfirm };
```

- `None`: ポップアップなし
- `Menu`: Move/Delete メニュー表示中
- `Move`: 挿入ライン移動モード
- `DeleteConfirm`: 削除確認ポップアップ

### 新規 enum — ManageFeedsPopup

```cpp
enum class ManageFeedsPopup { None, Menu, Move, DeleteConfirm, DiscardConfirm };
```

- `DiscardConfirm`: Exit 時の破棄確認ポップアップ

### AppState 追加フィールド

```cpp
// --- FeedList ポップアップ ---
FeedListPopup feedListPopup = FeedListPopup::None;
int feedListPopupMenuSel = 0;   // ポップアップ内カーソル (0=Move, 1=Delete)
int feedListPopupTarget = -1;   // 操作対象フィードインデックス
int feedListMoveInsertPos = 0;  // 挿入ライン位置 (0..feedCount)

// --- ManageFeeds 画面 ---
std::vector<FeedConfig> manageFeedsEditing; // feedConfigs の編集コピー
int manageFeedsSelected = 0;               // リストカーソル
bool manageFeedsDirty = false;             // 未保存変更あり
ManageFeedsPopup manageFeedsPopup = ManageFeedsPopup::None;
int manageFeedsPopupMenuSel = 0;           // ポップアップ内カーソル (0=Move, 1=Delete)
int manageFeedsPopupTarget = -1;           // 操作対象インデックス
int manageFeedsMoveInsertPos = 0;          // 挿入ライン位置 (0..editing.size())
```

### 状態フラグ ライフサイクル表

| 変数 | 初期値 | セット条件 | クリア条件 | 残留リスク |
|------|--------|-----------|-----------|-----------|
| `feedListPopup` | None | SELECT押下（フィード行のみ） | A/B確定・キャンセル・画面遷移 | Menu残留でFeedListが操作不能になる |
| `feedListPopupTarget` | -1 | SELECT押下時 | feedListPopup=None時 | 負値参照クラッシュ（範囲チェック必須） |
| `feedListMoveInsertPos` | 0 | Move モード開始時（popupTarget位置で初期化） | Move完了・キャンセル | — |
| `manageFeedsDirty` | false | Add/Move/Delete操作後 | Save完了・Discard確定 | trueのまま残るとExit時に不要確認が出る |
| `manageFeedsPopup` | None | A押下（フィード行のみ） | A/B確定・キャンセル | DiscardConfirm残留でManageFeedsが操作不能 |

---

## 3. ui.cpp — 描画変更

### 3-1. drawFeedList: 項目順変更・ポップアップ描画

**項目インデックス（変更前 → 変更後）**

```
変更前: feedCount+0=Bookmarks, feedCount+1=Settings  (total = feedCount+2)
変更後: feedCount+0=Bookmarks, feedCount+1=Add Feed, feedCount+2=Settings  (total = feedCount+3)
```

**描画ループ内ラベル生成（変更後）**

```cpp
int total = feedCount + 3;
// ...
if (i < feedCount) {
    snprintf(label, sizeof(label), "%s", state.feedConfigs[i].name.c_str());
    textClr = CLR_TEXT;
} else if (i == feedCount) {
    snprintf(label, sizeof(label), "\xe2\x98\x85 Bookmarks");
    textClr = CLR_TEXT;
} else if (i == feedCount + 1) {
    snprintf(label, sizeof(label), "[Add Feed]");
    textClr = CLR_HINT;
} else {
    snprintf(label, sizeof(label), "[Settings]");
    textClr = CLR_TEXT;
}
```

**Move モード描画（drawFeedList 内、ループの後）**

```cpp
if (state.feedListPopup == FeedListPopup::Move) {
    // 移動元をグレーアウト（CLR_HINTで上書き）
    // ループ内で target==i のとき color=CLR_HINT で描画する
    
    // 挿入ライン描画
    int pos = state.feedListMoveInsertPos; // 0..feedCount
    // リスト先頭行のY座標を計算し、pos番目の「間」にラインを引く
    float lineY = TEXT_MARGIN_Y + (float)(pos - start) * LINE_HEIGHT - 1.0f;
    if (lineY >= TEXT_MARGIN_Y - LINE_HEIGHT && lineY < BOT_H - 30.0f)
        C2D_DrawRectSolid(0, lineY, 0.1f, BOT_W, 2.0f, CLR_TITLE);
}
```

**SELECT ポップアップ描画（drawFeedList 内）**

```cpp
if (state.feedListPopup == FeedListPopup::Menu) {
    constexpr float PX = 40.0f, PY = 80.0f, PW = BOT_W - 80.0f, PH = 38.0f;
    C2D_DrawRectSolid(PX, PY, 0.1f, PW, PH, CLR_SEL_BG);
    const char* items[2] = {"Move", "Delete"};
    for (int i = 0; i < 2; ++i) {
        float y = PY + 4.0f + (float)i * LINE_HEIGHT;
        if (i == state.feedListPopupMenuSel)
            C2D_DrawRectSolid(PX, y - 1.0f, 0.05f, PW, LINE_HEIGHT + 1.0f, CLR_TITLE);
        drawText(items[i], PX + 8.0f, y, 0.05f, TEXT_SCALE, TEXT_SCALE, CLR_TEXT);
    }
}

if (state.feedListPopup == FeedListPopup::DeleteConfirm) {
    constexpr float DX = 30.0f, DY = 80.0f, DW = BOT_W - 60.0f, DH = 54.0f;
    C2D_DrawRectSolid(DX, DY, 0.1f, DW, DH, CLR_SEL_BG);
    drawText("Delete this feed?", DX + 8.0f, DY + 8.0f, 0.1f, TEXT_SCALE, TEXT_SCALE, CLR_TEXT);
    drawText("A:Yes  B:Cancel",   DX + 8.0f, DY + 28.0f, 0.1f, TEXT_SCALE, TEXT_SCALE, CLR_HINT);
}
```

### 3-2. drawSettings: Manage Feeds 項目追加

```cpp
// 変更前: items[3]、"Save" がインデックス2
// 変更後: items[4]、"Manage Feeds" がインデックス2、"Save" がインデックス3
const Item items[4] = {
    {"Scroll Delay",    state.settings.scrollRepeatDelayMs,    false},
    {"Scroll Interval", state.settings.scrollRepeatIntervalMs, false},
    {"Manage Feeds",    0,                                      true},
    {"Save",            0,                                      true},
};
// ループ上限を 4 に変更
// ガイドテキスト: "Up/Down:select  L/R:change  A:select  B:back"
```

### 3-3. drawManageFeeds: 新規関数

```
上画面: "Manage Feeds" タイトル（CLR_TITLE）+ statusMsg（CLR_ERROR、失敗時）
下画面:
  [Feed 1]  ... フィード群（manageFeedsEditing）
  [Add Feed]
  Save
  Exit
  フッター: "Up/Down:move  A:select  B:exit"
```

- フィード行: A → ポップアップ (Menu)
- 移動対象フィードは CLR_HINT でグレーアウト、挿入ラインは CLR_TITLE
- Menu/DeleteConfirm/DiscardConfirm ポップアップは BookmarkList の既存パターンと同形式

---

## 4. ui.cpp — 入力処理変更

### 4-1. FeedList 入力（case Screen::FeedList）

**項目数更新**

```cpp
int total = feedCount + 3; // Bookmarks, Add Feed, Settings
```

**A ボタン処理（変更後）**

```cpp
if (kDown & KEY_A) {
    if (state.selectedFeed == feedCount + 2) {
        // Settings
        state.currentScreen = Screen::Settings;
        state.settingsSelectedItem = 0;
    } else if (state.selectedFeed == feedCount + 1) {
        // Add Feed: Swkbd でURL入力
        // → handleAddFeedSwkbd(state) を呼ぶ（後述）
    } else if (state.selectedFeed == feedCount) {
        // Bookmarks
        state.selectedBookmark = 0;
        state.currentScreen = Screen::BookmarkList;
    } else {
        // フィードを開く（既存ロジック）
    }
    kDown &= ~KEY_A;
}
```

**SELECT ボタン処理（追加）**

```cpp
if ((kDown & KEY_SELECT) && state.feedListPopup == FeedListPopup::None
    && state.selectedFeed < feedCount) {
    state.feedListPopup = FeedListPopup::Menu;
    state.feedListPopupMenuSel = 0;
    state.feedListPopupTarget = state.selectedFeed;
    kDown &= ~KEY_SELECT;
}
```

**FeedListPopup::Menu 処理（追加）**

```cpp
if (state.feedListPopup == FeedListPopup::Menu) {
    if (kRepeat & KEY_UP) && state.feedListPopupMenuSel > 0)
        --state.feedListPopupMenuSel;
    if ((kRepeat & KEY_DOWN) && state.feedListPopupMenuSel < 1)
        ++state.feedListPopupMenuSel;
    if (kDown & KEY_A) {
        if (state.feedListPopupMenuSel == 0) {
            // Move モード開始
            state.feedListPopup = FeedListPopup::Move;
            state.feedListMoveInsertPos = state.feedListPopupTarget;
        } else {
            // Delete 確認
            state.feedListPopup = FeedListPopup::DeleteConfirm;
        }
        kDown &= ~KEY_A;
    }
    if (kDown & KEY_B) {
        state.feedListPopup = FeedListPopup::None;
        kDown &= ~KEY_B;
    }
    break; // 他の入力を無視
}
```

**FeedListPopup::Move 処理（追加）**

挿入ライン位置は 0..feedCount（target位置を除いた有効挿入点）。

```cpp
if (state.feedListPopup == FeedListPopup::Move) {
    int n = feedCount; // フィード総数
    if (kRepeat & KEY_UP) {
        state.feedListMoveInsertPos =
            (state.feedListMoveInsertPos - 1 + n + 1) % (n + 1); // ループ
        // target と同義の隣接位置はスキップしない（視覚的に分かりやすい）
    }
    if (kRepeat & KEY_DOWN) {
        state.feedListMoveInsertPos = (state.feedListMoveInsertPos + 1) % (n + 1);
    }
    if (kDown & KEY_A) {
        // feedConfigs 内で target → insertPos に移動
        int src = state.feedListPopupTarget;
        int dst = state.feedListMoveInsertPos;
        // dst はライン位置（0..n）。src 削除後の dst を調整する
        FeedConfig moving = state.feedConfigs[src];
        state.feedConfigs.erase(state.feedConfigs.begin() + src);
        int actualDst = (dst > src) ? dst - 1 : dst;
        state.feedConfigs.insert(state.feedConfigs.begin() + actualDst, moving);
        // feeds/feedLoaded も同様にシャッフル
        Feed movingFeed = std::move(state.feeds[src]);
        bool movingLoaded = state.feedLoaded[src];
        state.feeds.erase(state.feeds.begin() + src);
        state.feedLoaded.erase(state.feedLoaded.begin() + src);
        state.feeds.insert(state.feeds.begin() + actualDst, std::move(movingFeed));
        state.feedLoaded.insert(state.feedLoaded.begin() + actualDst, movingLoaded);
        // 即時保存
        if (!saveFeedConfig("sdmc:/3ds/rssreader/feeds.json", state.feedConfigs))
            state.statusMsg = "Save failed.";
        // selectedFeed を更新
        state.selectedFeed = actualDst;
        state.feedListPopup = FeedListPopup::None;
        kDown &= ~KEY_A;
    }
    if (kDown & KEY_B) {
        state.feedListPopup = FeedListPopup::None;
        kDown &= ~KEY_B;
    }
    break;
}
```

**FeedListPopup::DeleteConfirm 処理（追加）**

```cpp
if (state.feedListPopup == FeedListPopup::DeleteConfirm) {
    if (kDown & KEY_A) {
        int idx = state.feedListPopupTarget;
        state.feedConfigs.erase(state.feedConfigs.begin() + idx);
        state.feeds.erase(state.feeds.begin() + idx);
        state.feedLoaded.erase(state.feedLoaded.begin() + idx);
        // selectedFeed がはみ出ないよう調整
        if (state.selectedFeed >= (int)state.feedConfigs.size())
            state.selectedFeed = std::max(0, (int)state.feedConfigs.size() - 1);
        if (!saveFeedConfig("sdmc:/3ds/rssreader/feeds.json", state.feedConfigs))
            state.statusMsg = "Save failed.";
        state.feedListPopup = FeedListPopup::None;
        kDown &= ~KEY_A;
    }
    if (kDown & KEY_B) {
        state.feedListPopup = FeedListPopup::None;
        kDown &= ~KEY_B;
    }
    break;
}
```

**Add Feed — Swkbd（静的ヘルパー）**

```cpp
static void handleAddFeedSwkbd(AppState& state, bool applyImmediately) {
    SwkbdState swkbd;
    char url[256] = {};
    swkbdInit(&swkbd, SWKBD_TYPE_NORMAL, 2, 255);
    swkbdSetHintText(&swkbd, "https://example.com/feed.rss");
    SwkbdButton btn = swkbdInputText(&swkbd, url, sizeof(url));
    if (btn != SWKBD_BUTTON_CONFIRM || url[0] == '\0')
        return;
    // バリデーション: http:// または https:// で始まるか
    if (strncmp(url, "http://", 7) != 0 && strncmp(url, "https://", 8) != 0) {
        state.statusMsg = "Invalid URL (must start with http:// or https://)";
        return;
    }
    FeedConfig cfg;
    cfg.url = url;
    cfg.name = url; // 名前はURLで初期化（Rename機能は別フェーズ）
    cfg.fetch_full_text = false;

    if (applyImmediately) {
        // FeedList から追加: feedConfigs/feeds/feedLoaded を拡張して即時保存
        state.feedConfigs.push_back(cfg);
        state.feeds.emplace_back();
        state.feedLoaded.push_back(false);
        if (!saveFeedConfig("sdmc:/3ds/rssreader/feeds.json", state.feedConfigs))
            state.statusMsg = "Save failed.";
    } else {
        // ManageFeeds から追加: editing コピーのみ更新
        state.manageFeedsEditing.push_back(cfg);
        state.manageFeedsDirty = true;
    }
}
```

### 4-2. Settings 入力（case Screen::Settings）

```cpp
// 変更前: items = 3 (Save がインデックス 2)
// 変更後: items = 4 (Manage Feeds がインデックス 2, Save がインデックス 3)
if ((kRepeat & KEY_UP) && state.settingsSelectedItem > 0)
    --state.settingsSelectedItem;
if ((kRepeat & KEY_DOWN) && state.settingsSelectedItem < 3) // 3 に変更
    ++state.settingsSelectedItem;

// L/R: インデックス 0,1 のみ対象（Manage Feeds, Save には無効）

// A 押下
if (kDown & KEY_A) {
    if (state.settingsSelectedItem == 3) { // Save
        settingsSave(state.settings);
        state.currentScreen = Screen::FeedList;
    } else if (state.settingsSelectedItem == 2) { // Manage Feeds
        state.manageFeedsEditing = state.feedConfigs; // コピー作成
        state.manageFeedsSelected = 0;
        state.manageFeedsDirty = false;
        state.manageFeedsPopup = ManageFeedsPopup::None;
        state.currentScreen = Screen::ManageFeeds;
    }
    kDown &= ~KEY_A;
}
// B → 保存せずに FeedList（変更なし）
```

### 4-3. ManageFeeds 入力（case Screen::ManageFeeds — 新規）

```
manageFeedsEditing.size() = M
リストアイテム数 = M + 3 (Add Feed, Save, Exit)
インデックス M   = Add Feed
インデックス M+1 = Save
インデックス M+2 = Exit
```

```cpp
case Screen::ManageFeeds: {
    int M = (int)state.manageFeedsEditing.size();
    int listTotal = M + 3;

    if (state.manageFeedsPopup == ManageFeedsPopup::DiscardConfirm) {
        if (kDown & KEY_A) {
            state.manageFeedsPopup = ManageFeedsPopup::None;
            state.currentScreen = Screen::Settings;
            kDown &= ~KEY_A;
        }
        if (kDown & KEY_B) {
            state.manageFeedsPopup = ManageFeedsPopup::None;
            kDown &= ~KEY_B;
        }
        break;
    }
    if (state.manageFeedsPopup == ManageFeedsPopup::Menu) {
        // FeedList と同様の Menu 処理（Move/Delete）
        // Move → ManageFeedsPopup::Move, Delete → ManageFeedsPopup::DeleteConfirm
        break;
    }
    if (state.manageFeedsPopup == ManageFeedsPopup::Move) {
        // FeedList と同様（editing コピーを操作、dirty = true）
        break;
    }
    if (state.manageFeedsPopup == ManageFeedsPopup::DeleteConfirm) {
        // FeedList と同様（editing コピーを操作、dirty = true）
        break;
    }

    // 通常入力
    if ((kRepeat & KEY_UP) && state.manageFeedsSelected > 0)
        --state.manageFeedsSelected;
    if ((kRepeat & KEY_DOWN) && state.manageFeedsSelected < listTotal - 1)
        ++state.manageFeedsSelected;

    if (kDown & KEY_A) {
        int sel = state.manageFeedsSelected;
        if (sel < M) {
            // フィード行 → ポップアップ
            state.manageFeedsPopup = ManageFeedsPopup::Menu;
            state.manageFeedsPopupMenuSel = 0;
            state.manageFeedsPopupTarget = sel;
        } else if (sel == M) {
            // Add Feed
            handleAddFeedSwkbd(state, false);
        } else if (sel == M + 1) {
            // Save
            state.feedConfigs = state.manageFeedsEditing;
            state.feeds.resize(state.feedConfigs.size());
            state.feedLoaded.assign(state.feedConfigs.size(), false);
            if (!saveFeedConfig("sdmc:/3ds/rssreader/feeds.json", state.feedConfigs))
                state.statusMsg = "Save failed.";
            else
                state.currentScreen = Screen::Settings;
        } else {
            // Exit
            if (state.manageFeedsDirty)
                state.manageFeedsPopup = ManageFeedsPopup::DiscardConfirm;
            else
                state.currentScreen = Screen::Settings;
        }
        kDown &= ~KEY_A;
    }
    if (kDown & KEY_B) {
        if (state.manageFeedsDirty)
            state.manageFeedsPopup = ManageFeedsPopup::DiscardConfirm;
        else
            state.currentScreen = Screen::Settings;
        kDown &= ~KEY_B;
    }
    break;
}
```

---

## 5. main.cpp — kRepeat 条件追加

```cpp
// 変更前
if (state.currentScreen == Screen::ArticleView) { ... settings speed }
else { 200, 167 }

// 変更後
if (state.currentScreen == Screen::ArticleView) { ... settings speed }
else { computeRepeat(kDown, kHeld, repeatState, 200, 167); }
// ManageFeeds/FeedList の Move モードも 200/167 固定（RDD: リスト選択と同期）
```

※ 変更なし（現行の else ブランチで ManageFeeds も自動的にカバーされる）

---

## 6. 実装順序とビルドチェックポイント

1. `feed_config.h/cpp` — `saveFeedConfig` 追加 → ビルド確認
2. `ui.h` — `Screen::ManageFeeds`, enum 2種, AppState フィールド追加 → ビルド確認（make clean）
3. `ui.cpp` — `drawFeedList` 項目順変更のみ（ポップアップなし） → ビルド確認
4. `ui.cpp` — FeedList A ボタン処理更新（Add Feed / Bookmarks / Settings 再マッピング）
5. `ui.cpp` — FeedList SELECT + Menu + Move + DeleteConfirm 入力処理
6. `ui.cpp` — FeedList Move モード描画（挿入ライン・グレーアウト）
7. `ui.cpp` — `drawSettings` 更新（Manage Feeds 項目追加）
8. `ui.cpp` — Settings 入力処理更新（Manage Feeds A押し）
9. `ui.cpp` — `drawManageFeeds` 新規関数 + `uiDraw` switch 追加
10. `ui.cpp` — ManageFeeds 入力処理（Menu/Move/DeleteConfirm/DiscardConfirm/Save/Exit）
11. 結合動作確認

---

## 7. 注意事項

- `make clean && make` が必要（ui.h の struct 変更を含む）
- Swkbd 呼び出しはメインスレッドのみ（workerMain からは呼ばない）
- Move 確定時: `feeds` / `feedLoaded` も `feedConfigs` と同順に並び替えること（インデックスズレ防止）
- ManageFeeds の Save 時: `feedLoaded` を全 false にリセット（editing は変更済みURLを含む可能性）
- `feedListPopupTarget` は範囲チェック（`feedConfigs.erase` 後にアクセスしない）
