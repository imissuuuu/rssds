# システム設計書 - Phase 5 Font Fallback
**RDD:** `archive/RDD-2026-04-11-1400.md`
**作成日:** 2026-04-13

## 1. 全体方針

1. **フォント管理は ui.cpp に閉じる** — 新ファイル不要。`static C2D_Font fallbackFont` を追加するだけで完結。`ui.h` への変更はゼロ。
2. **区間分割描画** — `C2D_TextParse`（システムフォント）と `C2D_TextFontParse`（補完フォント）の同一シーン混在は **実機で動作確認済み**（2026-04-13 実験）。citro2d バグは再現せず。
3. **C2D_Text キャッシュなし** — coding-patterns #7 に従い毎フレーム `TextParse`。
4. **フォント未ロード時は既存動作を維持** — `fallbackFont == nullptr` なら分割せず既存パスをそのまま通す。
5. **mkbcfnt はビルド時に自動実行** — Makefile ルールで TTF + whitelist → `romfs/fallback.bcfnt` を生成。

---

## 2. 新規ファイル

### `assets/fonts/symbols_whitelist.txt`
```
← → ↑ ↓ ↔ ⇒ ⇐
★ ☆ ● ○ ■ □ ◆ ◇ ▲ ▼ ◀ ▶
… · ※ ‐ – — ' ' " "
℃ ± × ÷ ° ′ ″ § ¶ † ‡ ‰
€ £ ¥ ¢
≠ ≤ ≥ ≈ ∞ ∑ √ ∫
✓ ✗ ☐ ☑
♠ ♥ ♦ ♣
♪ ♫ ♭ ♯
```
初期 ~60 文字。実機で不足したら追加してビルド。

### `assets/fonts/NotoSansSymbols2-Regular.ttf`
Google Noto Sans Symbols 2 (SIL OFL 1.1)。手動ダウンロードして配置。

### `assets/fonts/FONT_LICENSE.txt`
SIL OFL 1.1 ライセンス本文。

### `romfs/fallback.bcfnt`
ビルド生成物。`.gitignore` に追加。

---

## 3. 既存ファイルの変更

### `source/ui.cpp`

**static 変数追加（`measureBuf` の直後）:**
```cpp
static C2D_Font fallbackFont = nullptr;
```

**`uiInit()` 拡張:**
```cpp
void uiInit() {
    topTarget  = C2D_CreateScreenTarget(GFX_TOP,    GFX_LEFT);
    botTarget  = C2D_CreateScreenTarget(GFX_BOTTOM, GFX_LEFT);
    textBuf    = C2D_TextBufNew(4096);
    measureBuf = C2D_TextBufNew(512);
    fallbackFont = C2D_FontLoad("romfs:/fallback.bcfnt");
    if (!fallbackFont) {
        fprintf(stderr, "[ui] fallback font not loaded\n");
    }
}
```

**`uiExit()` 拡張:**
```cpp
void uiExit() {
    if (fallbackFont) {
        C2D_FontFree(fallbackFont);
        fallbackFont = nullptr;
    }
    C2D_TextBufDelete(measureBuf);
    measureBuf = nullptr;
    C2D_TextBufDelete(textBuf);
    textBuf = nullptr;
}
```

**新規ヘルパー（`drawText` の直前に追加）:**
```cpp
// UTF-8 1文字をデコードしコードポイントとバイト数を返す。
// 不正バイトは U+FFFD として 1 バイト進める。
static uint32_t utf8Decode(const char* s, int& outBytes) {
    unsigned char c = (unsigned char)s[0];
    if (c < 0x80)              { outBytes = 1; return c; }
    if ((c & 0xE0) == 0xC0)   { outBytes = 2; return ((c & 0x1F) << 6)  | ((unsigned char)s[1] & 0x3F); }
    if ((c & 0xF0) == 0xE0)   { outBytes = 3; return ((c & 0x0F) << 12) | (((unsigned char)s[1] & 0x3F) << 6) | ((unsigned char)s[2] & 0x3F); }
    if ((c & 0xF8) == 0xF0)   { outBytes = 4; return ((c & 0x07) << 18) | (((unsigned char)s[1] & 0x3F) << 12) | (((unsigned char)s[2] & 0x3F) << 6) | ((unsigned char)s[3] & 0x3F); }
    outBytes = 1; return 0xFFFD;
}

// システムフォントにグリフがあるか（alterCharIndex と一致しなければあり）
static bool systemHasGlyph(uint32_t cp) {
    CFNT_s* sys   = fontGetSystemFont();
    int     alter = sys->finf.alterCharIndex;
    return fontGlyphIndexFromCodePoint(sys, cp) != alter;
}
```

**`drawText` の置換:**

変更前:
```cpp
static void drawText(const char* str, float x, float y, float z,
                     float sx, float sy, u32 color) {
    C2D_Text text;
    C2D_TextParse(&text, textBuf, str);
    C2D_TextOptimize(&text);
    C2D_DrawText(&text, C2D_WithColor, x, y, z, sx, sy, color);
}
```

変更後:
```cpp
static void drawText(const char* str, float x, float y, float z,
                     float sx, float sy, u32 color) {
    if (!fallbackFont) {
        C2D_Text text;
        C2D_TextParse(&text, textBuf, str);
        C2D_TextOptimize(&text);
        C2D_DrawText(&text, C2D_WithColor, x, y, z, sx, sy, color);
        return;
    }
    float cursorX = x;
    size_t n = strlen(str), i = 0;
    while (i < n) {
        int bytes;
        uint32_t cp  = utf8Decode(str + i, bytes);
        bool useSys  = systemHasGlyph(cp);
        size_t j     = i + bytes;
        while (j < n) {
            int nb; uint32_t ncp = utf8Decode(str + j, nb);
            if (systemHasGlyph(ncp) != useSys) break;
            j += nb;
        }
        std::string chunk(str + i, j - i);
        C2D_Text text;
        if (useSys) C2D_TextParse     (&text, textBuf, chunk.c_str());
        else        C2D_TextFontParse (&text, fallbackFont, textBuf, chunk.c_str());
        C2D_TextOptimize(&text);
        C2D_DrawText(&text, C2D_WithColor, cursorX, y, z, sx, sy, color);
        cursorX += text.width * sx;
        i = j;
    }
}
```

**`measureStr` の置換:**

変更前:
```cpp
static float measureStr(const char* str, float scale) {
    C2D_TextBufClear(measureBuf);
    C2D_Text t;
    C2D_TextParse(&t, measureBuf, str);
    return t.width * scale;
}
```

変更後:
```cpp
static float measureStr(const char* str, float scale) {
    C2D_TextBufClear(measureBuf);
    if (!fallbackFont) {
        C2D_Text t;
        C2D_TextParse(&t, measureBuf, str);
        return t.width * scale;
    }
    float total = 0.0f;
    size_t n = strlen(str), i = 0;
    while (i < n) {
        int bytes;
        uint32_t cp = utf8Decode(str + i, bytes);
        bool useSys = systemHasGlyph(cp);
        size_t j    = i + bytes;
        while (j < n) {
            int nb; uint32_t ncp = utf8Decode(str + j, nb);
            if (systemHasGlyph(ncp) != useSys) break;
            j += nb;
        }
        std::string chunk(str + i, j - i);
        C2D_Text t;
        if (useSys) C2D_TextParse     (&t, measureBuf, chunk.c_str());
        else        C2D_TextFontParse (&t, fallbackFont, measureBuf, chunk.c_str());
        total += t.width;
        i = j;
    }
    return total * scale;
}
```

### `source/main.cpp`

`gfxInitDefault()` の直後に `romfsInit()` を追加:
```cpp
gfxInitDefault();
romfsInit();
C3D_Init(C3D_DEFAULT_CMDBUF_SIZE);
```

### `Makefile`

`ROMFS := romfs` は既設。トップレベルセクションにフォント生成ルールを追加:

```make
FALLBACK_FONT   := $(CURDIR)/romfs/fallback.bcfnt
FONT_TTF        := $(CURDIR)/assets/fonts/NotoSansSymbols2-Regular.ttf
FONT_WHITELIST  := $(CURDIR)/assets/fonts/symbols_whitelist.txt

$(FALLBACK_FONT): $(FONT_TTF) $(FONT_WHITELIST)
	@echo "mkbcfnt: generating fallback.bcfnt"
	@mkbcfnt -w $(FONT_WHITELIST) -o $@ $(FONT_TTF)

all: $(FALLBACK_FONT)
```

`clean` ターゲットに `$(FALLBACK_FONT)` を追加。

### `.gitignore`

```
romfs/fallback.bcfnt
```

---

## 4. 各機能の詳細設計

### FR-2: 区間分割の動作

```
入力: "ITmedia★ニュース→特集"
走査:
  "ITmedia"  → systemHasGlyph=true  → システムフォントで描画
  "★"        → systemHasGlyph=false → 補完フォントで描画
  "ニュース"  → systemHasGlyph=true  → システムフォントで描画
  "→"        → systemHasGlyph=false → 補完フォントで描画
  "特集"      → systemHasGlyph=true  → システムフォントで描画
```

### FR-3: メトリクス整合

まずデフォルトサイズで変換し、実機でシステムフォントとのベースライン差を確認。ずれがあれば `mkbcfnt -s <N>` で調整。

### FR-4: 三段フォールバック

1. `systemHasGlyph(cp) == true` → `C2D_TextParse`
2. false → `C2D_TextFontParse`（whitelist になければ補完フォントの alterChar）
3. どちらも無し → 補完フォントの `？` 相当（許容）

---

## 5. 実装順序

| 段階 | 作業 | ビルド確認 |
|------|------|-----------|
| 1 | `assets/fonts/` 作成、TTF・whitelist・LICENSE 配置 | なし |
| 2 | `.gitignore` に `romfs/fallback.bcfnt` 追加 | なし |
| 3 | Makefile にフォント生成ルール追加 | `make` → `romfs/fallback.bcfnt` 生成確認 |
| 4 | `main.cpp` に `romfsInit()` 追加 | `make` → ビルド通る |
| 5 | `ui.cpp`: `fallbackFont` 変数・`uiInit`/`uiExit` 拡張のみ（drawText は未変更） | `make` → 起動して既存画面が壊れないこと確認 |
| 6 | `ui.cpp`: `utf8Decode`/`systemHasGlyph`/`drawText`/`measureStr` 置換 | `make clean && make` → 記号を含む記事で表示確認 |

---

## 6. 影響ファイル一覧

| ファイル | 変更種別 | 内容 |
|----------|----------|------|
| `source/ui.cpp` | 変更 | フォント変数・初期化・区間分割描画ロジック |
| `source/main.cpp` | 変更 | `romfsInit()` 追加 |
| `Makefile` | 変更 | `mkbcfnt` 生成ルール、`all`/`clean` 依存更新 |
| `.gitignore` | 変更 | `romfs/fallback.bcfnt` 追加 |
| `assets/fonts/NotoSansSymbols2-Regular.ttf` | 新規 | ソースフォント（手動配置） |
| `assets/fonts/symbols_whitelist.txt` | 新規 | 対象コードポイント |
| `assets/fonts/FONT_LICENSE.txt` | 新規 | SIL OFL ライセンス |
| `romfs/fallback.bcfnt` | 新規（生成物） | ビルド時自動生成 |
